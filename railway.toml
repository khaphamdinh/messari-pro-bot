[build]
builder = "nixpacks"
buildCommand = "cp -n src/prompts.example.ts src/prompts.ts && npm run build"

[deploy]
startCommand = "npm run start:all"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
internalPort = 3000
